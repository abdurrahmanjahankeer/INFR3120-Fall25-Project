<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
    <a class="navbar-brand d-flex align-items-center" href="/">
      <img src="/Asset/images/ADA-Type-Racer.png" alt="ADA Type Racer" height="50" class="me-2">
      <span>ADA Type Writer</span>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <% if(title=="Play") { %>
            <a class="nav-link active" aria-current="page" href="/game"><i class="fa-solid fa-lg fa-keyboard"></i> Play</a>
          <% } else { %>
            <a class="nav-link" aria-current="page" href="/game"><i class="fa-solid fa-lg fa-keyboard"></i> Play</a>
          <% } %>
        </li>
        <li class="nav-item">
          <% if(title==="Leaderboard") { %>
            <a class="nav-link active" href="/typingRecords"><i class="fa-solid fa-trophy"></i> Leaderboard</a>
          <% } else { %>
            <a class="nav-link" href="/typingRecords"><i class="fa-solid fa-trophy"></i> Leaderboard</a>
          <% } %>
        </li>
        <li class="nav-item">
          <% if(title=="About") { %>
            <a class="nav-link active" aria-current="page" href="/about"><i class="fa-solid fa-lg fa-circle-info"></i> About</a>
          <% } else { %>
            <a class="nav-link" aria-current="page" href="/about"><i class="fa-solid fa-lg fa-circle-info"></i> About</a>
          <% } %>
        </li>
        <li class="nav-item">
          <% if(title=="Contact") { %>
            <a class="nav-link active" aria-current="page" href="/contact"><i class="fa-solid fa-lg fa-phone"></i> Contact</a>
          <% } else { %>
            <a class="nav-link" aria-current="page" href="/contact"><i class="fa-solid fa-lg fa-phone"></i> Contact</a>
          <% } %>
        </li>
        <% if(typeof displayName=='string' && displayName) { %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle"
              href="#"
              id="userDropdown"
              role="button"
              data-bs-toggle="dropdown"
              aria-expanded="false">
              <% if (typeof profileImageUrl !== 'undefined' && profileImageUrl) { %>
                <img src="<%= profileImageUrl %>"
                    alt="Profile"
                    style="width:32px; height:32px; border-radius:50%; margin-right:6px;">
              <% } else { %>
                <i class="fas fa-lg fa-user-circle" style="margin-right:4px;"></i>
              <% } %>
              <span><%= displayName %></span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown" style="background-color: white; color: black;">
              <li>
                <a class="dropdown-item" href="/users/profile-picture">
                  <i class="fas fa-image"></i> Upload Profile Picture
                </a>
              </li>
              <li>
                <a class="dropdown-item" href="/users/change-password">
                  <i class="fas fa-key"></i> Change / Reset Password
                </a>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <a class="dropdown-item" href="/logout">
                  <i class="fas fa-sign-out-alt"></i> Logout
                </a>
              </li>
            </ul>
          </li>
        <% } else { %>
          <li class="nav-item">
            <% if(title==="Login") { %>
              <a class="nav-link active" href="/login">
                <i class="fas fa-lg fa-sign-in-alt"></i> Login
              </a>
            <% } else { %>
              <a class="nav-link" href="/login">
                <i class="fas fa-lg fa-sign-in-alt"></i> Login
              </a>
            <% } %>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>